# npm 包版本

1. npm 包版本

-   主版本号: 大版本更新，功能添加，向下不兼容
-   次版本号: 功能新增，向下兼容
-   补丁版本号: 修复 bug

1. 符号意义

-   ~ 会匹配最近的小版本依赖包，比如~1.2.3 会匹配所有 1.2.x 版本，但是不包括 1.3.0
-   ^ 会匹配最新的大版本依赖包，比如^1.2.3 会匹配所有 1.x.x 的包，包括 1.3.0，但是不包括 2.0.0
-   ✱ 这意味着安装最新版本的依赖包
